/**
 * O balão do Nuxt UI nasce com altura fixa e texto truncado, medida para rótulo de duas palavras.
 * Quem explica algo numa frase inteira passa isto, e o balão cresce em linhas em vez de esticar.
 */
export const WRAPPED_TOOLTIP = {
  content: 'h-auto max-w-xs items-start px-3 py-2',
  text: 'whitespace-normal overflow-visible leading-snug'
}
