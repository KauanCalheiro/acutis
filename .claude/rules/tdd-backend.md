---
paths:
  - "backend-laravel/app/**/*"
  - "backend/src/api/**/*"
---

**Invariant:** write the failing test **before** any code in `backend-laravel/app/` (Laravel) or `backend/src/api/` (o Node que o substitui). Never delete or disable a test without explicit user request.
