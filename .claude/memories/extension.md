---
name: extension
description: Extensão Chrome (extension/) — LER 1º ao mexer em background, content script, offscreen ou popup; índice → tdd
metadata:
  type: feedback
---

Índice da extensão (Manifest V3, Vite + Vue via `@crxjs/vite-plugin`). Ler a sub-memória do assunto antes de mexer. Estrutura de pastas em [[structure]].

## Sub-memórias

| Arquivo | Assunto |
|---------|---------|
| [[extension-tdd]] | Teste SEMPRE antes da implementação — Vitest (lógica pura) + Playwright (E2E real) |
