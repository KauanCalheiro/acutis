{
  "name": "@acutis/e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter acutis build",
    "pretest": "node ../bin/ensure-chromium.js && pnpm build",
    "test": "playwright test",
    "pretest:smoke": "node ../bin/ensure-chromium.js && pnpm build",
    "test:smoke": "playwright test --grep @read",
    "capture:fixture": "pnpm --filter acutis build && node scripts/capture-sample-recording.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
