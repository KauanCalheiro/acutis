{
  "name": "acutis-e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "pnpm --dir ../extension build && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
