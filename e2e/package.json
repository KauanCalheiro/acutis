{
  "name": "acutis-e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "pnpm --dir ../webdriver build:ui && pnpm --dir ../webdriver build && playwright test",
    "capture:fixture": "pnpm --dir ../webdriver build:ui && pnpm --dir ../webdriver build && node scripts/capture-sample-recording.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
