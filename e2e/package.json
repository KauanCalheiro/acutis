{
  "name": "acutis-e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "./scripts/setup.sh && pnpm --dir ../backend build:ui && pnpm --dir ../backend build",
    "pretest": "pnpm build",
    "test": "playwright test",
    "pretest:smoke": "pnpm build",
    "test:smoke": "playwright test --grep @read",
    "capture:fixture": "pnpm --dir ../backend build:ui && pnpm --dir ../backend build && node scripts/capture-sample-recording.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
